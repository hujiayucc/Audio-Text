<resources>
    <style name="Base.Theme.AT" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/theme_dark</item>
        <item name="android:colorControlActivated">?attr/colorPrimary</item>
        <!-- 边框颜色 -->
        <item name="boxStrokeColor">?attr/colorPrimary</item>
        <!-- 获得焦点时的边框颜色 -->
        <item name="boxStrokeWidthFocused">?attr/colorPrimary</item>
        <item name="android:textColor">?attr/colorPrimary</item>
        <item name="android:editTextColor">?attr/colorPrimary</item>
        <item name="titleTextColor">@color/white</item>
        <item name="tabIndicatorColor">@android:color/holo_blue_dark</item>
    </style>
</resources>